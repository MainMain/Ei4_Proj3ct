var client  = require("../index").createClient(),
    util = require("util");

client.monitor(function (err, res) {
    EventLog.log("Entering monitoring mode.");
});

client.on("monitor", function (time, args) {
    EventLog.log(time + ": " + util.inspect(args));
});
